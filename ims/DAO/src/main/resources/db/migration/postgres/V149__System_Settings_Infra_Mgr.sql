-- author Intesar Mohammed
-- Supports storing and linking hypercloud infra_manager urls.

insert into app_config (id, key_, value_, description) values ('infra.manager.endpoints', 'infra.manager.endpoints', '', 'Comma separated values.');
