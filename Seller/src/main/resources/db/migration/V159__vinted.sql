CREATE TABLE public.vinted
(
  id character varying(255),
  item_id character varying(255),
  url character varying(255),
  title character varying(255),

  CONSTRAINT vinted_pkey PRIMARY KEY (id)
);

